import React from 'react';
import Article from './Article';

const AbundantLifePage = ({articleItems}) => {
    return (
        <Article articleItems={articleItems} />
    );
}

export default AbundantLifePage;
